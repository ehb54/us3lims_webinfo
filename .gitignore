# Deployment configuration, symlinked into place by the container entrypoint
# (or written per-host from config.php.template on a native install). Never
# versioned: each deployment supplies its own, and they carry site database
# credentials. config.php was committed once by mistake and removed again in
# aba463f; this file is what keeps that from recurring.
/config.php
/us3/

.DS_Store
.idea/
*.log
